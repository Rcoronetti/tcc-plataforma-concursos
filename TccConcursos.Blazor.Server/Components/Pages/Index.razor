@page "/"
@rendermode InteractiveServer
@inject NavigationManager Nav

@code {
    protected override Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
            Nav.NavigateTo("/concursos", replace: true);

        return Task.CompletedTask;
    }
}